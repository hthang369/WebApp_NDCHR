using System;
using System.Data;
using System.Text;
using System.Collections.Generic;
using ExpertWebLib;
namespace ExpertERP.BusinessEntities
{
	#region APOrdrPlns
	//-----------------------------------------------------------
	//Generated By: Expert Studio
	//Class:APOrdrPlnsController
	//Created Date:19 September 2012
	//-----------------------------------------------------------
	
	public class APOrdrPlnsController:BaseBusinessController
	{
		public APOrdrPlnsController()
		{
			dal= new DALBaseProvider("APOrdrPlns",typeof(APOrdrPlnsInfo));
		}
        public APOrdrPlnsInfo GetObjectByMonthAndYear(int iMonth, int iYear)
        {
            string strQuery = string.Format(@"Select top 1 * from APOrdrPlns where AAStatus='Alive' and APOrdrPlnPeriod={0} and APOrdrPlnYear={1}", iMonth, iYear);
            DataSet ds = dal.GetDataSet(strQuery);
            if (ds != null && ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0)
                return (APOrdrPlnsInfo)dal.GetObjectFromDataRow(ds.Tables[0].Rows[0]);
            return null;
        }
        public int CountOrdrPlanInPeriodAndYear(int iPeriod, int iYear)
        {
            String strQuery = String.Format(@"SELECT Count(*) FROM APOrdrPlns WHERE AAStatus = 'Alive' AND APOrdrPlnPeriod = {0} AND APOrdrPlnYear = {1}", iPeriod, iYear);
            DataSet ds = dal.GetDataSet(strQuery);
            if (ds != null && ds.Tables.Count > 0 && ds.Tables[0].Rows.Count > 0 && ds.Tables[0].Rows[0][0] != DBNull.Value)
                return Convert.ToInt32(ds.Tables[0].Rows[0][0]);
            return 0;
        }
	}
	#endregion
}